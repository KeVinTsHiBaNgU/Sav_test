<form method="post" action="{{ path('app_sav_delete', {'id': sav.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sav.id) }}">
    <button class="btn" class="btn btn-danger">Supprimer</button>
</form>
