{% extends 'base.html.twig' %}

{% block title %}Clients{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ client.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ client.prenom }}</td>
            </tr>
            <tr>
                <th>Adr1</th>
                <td>{{ client.adr1 }}</td>
            </tr>
            <tr>
                <th>Adr2</th>
                <td>{{ client.adr2 }}</td>
            </tr>
            <tr>
                <th>Codepos</th>
                <td>{{ client.codepos }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ client.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}" class="btn btn-secondary">retour</a>

    <a href="{{ path('app_client_edit', {'id': client.id}) }}" class="btn btn-primary">modifier</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
